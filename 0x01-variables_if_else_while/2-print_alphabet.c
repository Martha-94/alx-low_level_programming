#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char ch;

	for (ch = 'a'; ch <= 'z'; c++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
